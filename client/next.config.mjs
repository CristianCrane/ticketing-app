/** @type {import('next').NextConfig} */
const nextConfig = {
    // todo: consider this option if you find HMR isnt working
    // webpack: (config) => {
    //     config.watchOptions.poll = 300
    //     return config
    // }
};

export default nextConfig;
